# AirScan-QR 项目交接

> 更新时间：2026-08-21
>
> 仓库：`git@github.com:chping/airscan-qr.git`
>
> 分支：`main`
>
> 当前提交：`5d36b2d feat: add Firefox 68 compatible sender page`

## 1. 项目现状

AirScan-QR 是一个纯前端、无服务器的二维码喷泉码传输工具。发送端把文本或文件编码为持续刷新的二维码，接收端通过摄像头或屏幕捕获二维码并在本地解码。文件不会上传到服务器。

当前有两个入口：

| 页面 | 用途 | 浏览器基线 | 依赖方式 |
| --- | --- | --- | --- |
| `index.html` | 完整发送端和接收端 | 现代浏览器 | 引用 `lib/` 中的 QRious、jsQR 和 CSS |
| `sender.html` | 仅发送的单文件版本 | 面向 Firefox ESR 68.5 | QRious、CSS 和发送逻辑全部内联 |

线上地址：

- 完整版：<https://chping.github.io/airscan-qr/>
- 仅发送版：<https://chping.github.io/airscan-qr/sender.html>
- 仓库：<https://github.com/chping/airscan-qr>

以上两个页面已于 2026-08-21 验证返回当前应用内容。最新 Pages 工作流运行成功：<https://github.com/chping/airscan-qr/actions/runs/31317391610>。

## 2. 已确认的产品规则

### 2.1 传输方向

- 保持单向传输：数据只从发送端流向接收端。
- 不增加反向确认信道；发送端无法自动知道接收端是否已经完成。
- “完成本段”由发送端用户在确认接收端状态后手动点击。

### 2.2 文件分段

- 仅选择文件时启用分段；文本仍作为一次普通传输。
- 固定按 `256 KiB = 262,144` 字节切分。
- 最后一段使用实际剩余字节数，界面显示实际大小。
- 即使文件只有一段，也显示分段列表。
- 发送端列出全部分段，但只能按顺序传输。
- 当前段必须标记完成后，下一段才解锁；仅停止发送不算完成。
- 已完成的分段可以重新传输。
- 发送某段期间锁定文件选择、取消、页签切换等会改变传输上下文的操作。

### 2.3 接收和完整性

- 接收端自动接收并累积各段，不需要逐段手动启动。
- 发送端和接收端都显示分段列表及状态。
- 接收端只接受当前文件的下一段，拒绝跳段和其他文件的数据包。
- 每段使用 SHA-256 校验；校验失败时清空该段并重新接收。
- 全部分段校验通过后，接收端在内存中合并并下载完整文件。
- 接收状态不持久化，刷新页面后不能续传。
- 不兼容旧协议，不自动切换到另一文件；用户可主动放弃当前接收任务。
- 不设置文件总大小硬限制，实际容量受浏览器内存限制。

## 3. 当前技术实现

### 3.1 关键参数

| 参数 | 当前值 | 说明 |
| --- | --- | --- |
| `PROTOCOL_VERSION` | `ASQ3` | 当前二维码包协议 |
| `SEGMENT_SIZE` | `256 * 1024` | 文件分段大小，262,144 字节 |
| `FOUNTAIN_CHUNK_SIZE` | `220` | 喷泉码底层数据块大小 |
| 二维码数量 | 4 | 同时显示四个独立喷泉包 |
| 刷新率 | 15 FPS | 每轮刷新四个二维码 |
| QR 纠错等级 | L | 优先提高有效载荷 |

一个完整 256 KiB 分段会拆成 `ceil(262144 / 220) = 1192` 个喷泉块，最后一个喷泉块补齐后参与异或编码。

### 3.2 发送流程

1. 用户输入文本或选择文件。
2. 文件被切成 256 KiB 分段，生成分段列表。
3. 启动某段时，浏览器读取该文件切片并计算 SHA-256。
4. 文件切片再拆成 220 字节喷泉块。
5. 每个二维码包用种子伪随机选择若干喷泉块并异或。
6. 四个 QR canvas 以 15 FPS 持续显示不同的喷泉包。
7. 用户手动结束当前段并解锁下一段。

### 3.3 ASQ3 包格式

二维码内容由 12 个 `|` 分隔字段组成：

```text
协议版本 | 传输ID | 类型 | 段序号 | 段总数 | 随机种子 |
喷泉块总数 | 异或数据Base64 | URL编码文件名 | 文件总大小 |
本段实际大小 | 本段SHA-256
```

类型 `F` 表示文件，`T` 表示文本。段序号从 0 开始。

### 3.4 接收流程

1. `index.html` 使用摄像头或屏幕捕获视频。
2. 每个新视频帧扫描整帧和四个象限。
3. jsQR 使用 `inversionAttempts: "dontInvert"`，跳过反色重试。
4. 支持 `requestVideoFrameCallback` 时只扫描新的真实视频帧；否则回退到 `requestAnimationFrame`。
5. `handleFountainPacket` 校验协议、传输 ID 和分段顺序，并收集独立喷泉方程。
6. 解出当前段后执行 SHA-256 校验。
7. `finalizeSegment` 保存已验证段；全部完成后按顺序合并为 Blob 并下载。

## 4. 会话与版本演进

| 提交 | 会话结果 |
| --- | --- |
| `5f99817` | 初始仓库，仅有简短 README。 |
| `cfb7f46` | 建立纯前端喷泉码二维码文件传输应用。 |
| `5b8ff35` | 增加发送端手动分段、顺序解锁、接收端逐段校验和合并；当时为 ASQ2、512 KiB。 |
| `fe8baaa` | 增加 GitHub Actions 静态 Pages 部署。 |
| `11c616f` | 分段大小由 512 KiB 改为当前 256 KiB，协议升级为 ASQ3。 |
| `198da55` | 尝试双二维码及相关吞吐优化；实测没有明显改善。 |
| `7b657e3` | 按用户要求回退双二维码实验，恢复四二维码版本。 |
| `1678538` | 保留四二维码，提高到 15 FPS，并加入 `dontInvert` 和“仅扫描新视频帧”；实测速度明显改善。 |
| `5d36b2d` | 新增单文件、仅发送、面向 Firefox ESR 68.5 的 `sender.html`。 |

会话形成的关键结论：

- 一个大二维码的数据密度更高，但相机更难稳定识别；喷泉码允许丢包并持续恢复。
- 双二维码没有带来可感知提升，瓶颈不只在屏幕同时显示的二维码数量。
- 当前最有效的优化是四二维码、15 FPS、关闭反色扫描、避免重复扫描同一视频帧。
- 继续单纯提高 FPS 未必线性提速；相机实际帧率、曝光、屏幕刷新、二维码解码耗时和运动模糊都会成为瓶颈。

## 5. GitHub Pages 部署

`.github/workflows/static.yml` 在以下条件部署：

- 推送到 `main`；
- 手动触发 `workflow_dispatch`。

工作流上传仓库静态内容并部署到 GitHub Pages，不需要构建步骤。历史上曾出现“URL 返回 HTTP 200，但仍是旧 README/Jekyll 页面”的情况；后续已改用明确的 Pages Actions 工作流并解决。验收部署时必须检查页面标题和实际 HTML 内容，不能只看 HTTP 状态码。

## 6. Firefox ESR 68.5 状态

`sender.html` 是面向 Firefox ESR 68.5 的发送入口：

- 删除接收 UI、摄像头、屏幕捕获、jsQR 和接收状态机，而不是仅隐藏。
- QRious、样式和发送代码全部内联，无外部 `src` 或 `href` 依赖。
- 避免 `||=`、可选链 `?.` 和 `replaceAll`。
- 文件切片使用 `new Response(blob).arrayBuffer()` 读取。
- 已通过 Node 脚本语法解析、本地 HTTP 200 和现代浏览器四 canvas 文本发送冒烟检查。

`index.html` 仍使用 `replaceAll`、`||=`、可选链和 `Blob.arrayBuffer()` 等现代语法/API，不承诺兼容 Firefox ESR 68.5；旧浏览器应只使用 `sender.html` 发送。

当前最大未闭环项：修复后的 `sender.html` 尚缺少在真实 Firefox ESR 68.5 中完成“文件选择、SHA-256、四二维码生成”的最终实机证据。

## 7. 已验证与待验证

### 已验证

- 当前 `main` 与 `origin/main` 均指向 `5d36b2d`。
- 当前完整版和仅发送版 Pages 页面可访问并返回应用内容。
- `sender.html` 为真正单文件，不依赖 `lib/`。
- 现代浏览器文本发送可生成四个 300 × 300 QR canvas。
- 早期 512 KiB 版本曾验证 `524,289` 字节分成 `[524288, 1]`，协议字段、文本、SHA-256 和重组自检通过；该边界结果仅适用于历史 ASQ2 版本，不能替代当前 256 KiB 的验证。

### 下一项建议验收

在真实 Firefox ESR 68.5 使用 `sender.html`，选择一个 `262,145` 字节文件，并由现代浏览器的 `index.html` 接收：

1. 发送端应显示两段，大小分别为 `262,144` 和 `1` 字节。
2. 第二段在第一段完成前保持锁定。
3. 每段发送时应出现四个持续刷新的二维码。
4. 接收端应按顺序完成两段 SHA-256 校验。
5. 下载文件应与原文件逐字节一致。

可用以下命令核对原文件和下载文件：

```bash
shasum -a 256 /path/to/original /path/to/downloaded
```

验收记录至少保留 Firefox 版本、控制台错误、两段实际大小和两个文件的 SHA-256。

## 8. 已知问题和维护注意事项

- 没有反向 ACK：“完成本段”是人工确认，不代表协议层已经收到接收端确认。
- 接收端把分段和最终文件都保存在内存中，大文件可能触发浏览器内存压力。
- 刷新页面会丢失未完成任务，当前设计明确不做持久化或断点续传。
- `lib/html5-qrcode.min.js` 当前为空文件，`index.html` 仍引用它，但实际扫描使用 jsQR；这是可清理项，不影响当前主流程。
- `README.md` 仍非常简短，项目的完整技术和交接信息以本文档为准。
- 修改协议字段、分段大小或哈希规则时必须同步更新 `index.html` 和 `sender.html`，并升级协议版本以避免混收。
- 不要重新引入双二维码实验，除非有同设备、同文件、同拍摄条件下的基准数据证明收益。

## 9. 本地运行与快速检查

无需安装依赖：

```bash
cd /Users/chengping/code/github/airscan-qr
python3 -m http.server 8765
```

然后访问：

- 完整版：<http://localhost:8765/>
- 仅发送版：<http://localhost:8765/sender.html>

查看仓库状态：

```bash
git status --short
git log --oneline --decorate -10
```

## 10. 交接结论

当前可交付基线是：四二维码、15 FPS、ASQ3、256 KiB 手动顺序分段、逐段 SHA-256、接收端自动重组；`index.html` 提供完整功能，`sender.html` 提供单文件旧 Firefox 发送能力。下一位维护者应优先完成真实 Firefox ESR 68.5 的 256 KiB 边界端到端验收，再考虑其他性能或兼容性改动。
